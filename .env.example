# CryptoBot Environment Configuration
# Copy this file to .env and update with your values

# ============================================
# PERSISTENCE & DATABASE
# ============================================
# Redis for price/candle data caching (time-series)
REDIS_URL=redis://127.0.0.1:6379

# PostgreSQL for position tracking and P&L (transactional)
# Local development (requires docker-compose up):
DATABASE_URL=postgres://cryptobot:cryptobot_dev_password@localhost:5432/cryptobot
# Railway: Provided automatically by Railway Postgres plugin

# ============================================
# API KEYS
# ============================================
# DexScreener: Real-time price data (FREE - no API key needed)
# Used for fetching current prices every 5 minutes

# Birdeye: Token discovery only (trending tokens)
# Get free API key at https://birdeye.so
# Standard tier: 1 RPS, 30k credits/month
# Used for discovering trending tokens every 30 minutes
BIRDEYE_API_KEY=your_birdeye_api_key_here

# CoinGecko: Historical data backfill (OPTIONAL)
# Get API key at https://www.coingecko.com/en/api/pricing
# Demo tier: 10k calls/month, good for backfilling
# Note: Backfill is optional - bot works without it but takes 24h to collect data
COINGECKO_API_KEY=your_coingecko_api_key_here

# ============================================
# PORTFOLIO CONFIGURATION
# ============================================
# Initial portfolio value in USD (optional, defaults to 10000.0)
INITIAL_PORTFOLIO_VALUE=10000.0

# ============================================
# LOGGING
# ============================================
# Logging level (optional, defaults to info)
RUST_LOG=info
# For debug mode: RUST_LOG=debug
# For specific modules: RUST_LOG=cryptobot=debug,cryptobot::strategy=trace
